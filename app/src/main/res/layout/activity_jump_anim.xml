<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:orientation="vertical"
    tools:context="set.dhy.com.dhyset.JumpActivityAnims.JumpAnimActivity">

    <TextView
        android:id="@+id/explode"
        style="@style/homeItemStyle"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@drawable/city_item_left_bg"
        android:gravity="center"
        android:text="Explode是下一个页面的元素从四面八方进入"
        android:textColor="@color/white" />

    <TextView
        android:id="@+id/slide"
        style="@style/homeItemStyle"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@drawable/city_item_left_bg"
        android:gravity="center"
        android:text="Slide就是下一个页面元素从底部依次向上运动"
        android:textColor="@color/white" />

    <TextView
        android:id="@+id/fade"
        style="@style/homeItemStyle"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@drawable/city_item_left_bg"
        android:gravity="center"
        android:text="Fade就是下一个页面元素渐变出现"
        android:textColor="@color/white" />

    <TextView
        android:id="@+id/share"
        style="@style/homeItemStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/city_item_left_bg"
        android:gravity="center"
        android:text="Share是最复杂的一种转场方式，在跳转的两个Activity之间，如果有相同的View元素，那么，两个元素就可以设置成共享状态，在跳转时，这个View就会从第一个Activity的显示状态过渡到第二个Activity的显示状态，给用户的感觉仿佛是两个Activity共享一个View"
        android:textColor="@color/white" />

    <ImageView
        android:id="@+id/iv"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/ad" />

    <TextView
        android:id="@+id/tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="这是第四种动画"
        android:textColor="@color/textred"/>
</LinearLayout>
